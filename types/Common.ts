export interface IconProps {
     color?: string;
     className?: string;
}
export interface ProfileProps {
     id: number | null;
     name: string;
     profile_pic: string;
     user_name: string;
     contact: string;
}